const blogForm = document.getElementById("blogForm"); const message = document.getElementById("message"); const blogList = document.getElementById("blogList");
async function loadBlogs() { const response = await fetch("/api/blogs"); const { blogs } = await response.json(); blogList.replaceChildren(...blogs.map((blog) => { const article = document.createElement("article"); article.className = "blog-entry"; const heading = document.createElement("h3"); heading.textContent = blog.title; const text = document.createElement("p"); text.textContent = blog.content; const author = document.createElement("small"); author.textContent = `By ${blog.authorEmail} · ${new Date(blog.createdAt).toLocaleDateString()}`; article.append(heading, text, author); return article; })); }
blogForm.addEventListener("submit", async (event) => { event.preventDefault(); const token = localStorage.getItem("steppiToken"); if (!token) { message.textContent = "Please log in before publishing a blog."; return; } try { const response = await fetch("/api/blogs", { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify({ title: title.value, content: content.value }) }); const data = await response.json(); if (!response.ok) throw new Error(data.message); message.textContent = data.message; blogForm.reset(); loadBlogs(); } catch (error) { message.textContent = error.message || "Unable to publish blog."; } });
loadBlogs();
